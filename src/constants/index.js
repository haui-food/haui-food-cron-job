const RENDER_API = 'https://api.render.com/v1/services';

const RENDER_SERVICE_ACTION = {
  DEPLOY: 'deploy',
  RESUME: 'resume',
  SUSPEND: 'suspend',
  RESTART: 'restart',
};

module.exports = {
  RENDER_API,
  RENDER_SERVICE_ACTION,
};
